# Telegram-бот
aiogram==3.30.0

# LLM-провайдеры
google-genai==2.17.0        # Gemini — основной провайдер
# openai==2.x               # Этап 2: DeepSeek + Kimi (OpenAI-совместимые), опц. GPT
# anthropic==0.x            # Этап 5: Claude-провайдер (только при включении)

# Изображения
pillow==12.3.0

# База данных
SQLAlchemy[asyncio]==2.0.51
aiosqlite==0.22.1
# Postgres на хостинге: диск на бесплатных тарифах эфемерный, файл SQLite
# исчезает при каждом деплое вместе с ключами и историей пользователей.
asyncpg==0.31.0
alembic==1.19.1

# HTTP-сервер вебхука. Ставится и как зависимость aiogram, но мы импортируем
# aiohttp.web напрямую (bot.py) — значит, зависимость прямая.
aiohttp==3.14.3

# Конфигурация
pydantic-settings==2.15.0
python-dotenv==1.2.2

# Тестирование
pytest==9.1.1
pytest-asyncio==1.4.0
